interface:
  display_name: "Code Review Recursively"
  short_description: "PRの重大なレビュー指摘を修正し再レビューまで反復します"
  default_prompt: "Use $code-review-recursively to review the current PR and fix all CRITICAL and HIGH findings."
