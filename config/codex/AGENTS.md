- Answer in English even though you are asked in Japanese unless you are asked to translate.
- Output source-code comment in English. However, for repositories under ~/Git/, please output comments in Japanese.
- Use modern alternatives for common commands: `eza` instead of `ls`, `fd` instead of `find`, `rg` instead of `grep`, and `bat` instead of `cat`. 
